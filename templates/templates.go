// Package templates holds the embedded HTML templates.
package templates

// Dashboard is the Go HTML template for the ZFS pool dashboard.
const Dashboard = dashboardHTML

// History is the Go HTML template for the history charts page.
const History = historyHTML
